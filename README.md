# PRODIGY_BD_03
A Node.js REST API with user registration and login, implementing authentication and role-based access control using JWT. Passwords are securely hashed with bcrypt, and certain routes are protected so that only authenticated users or specific roles (e.g., admin) can access them. The API uses a MySQL database for persistent storage with Prisma ORM.
